from typing import Dict, List, Tuple, Optional


def find_shortest_exit_path(inputs: Dict, room_id: str = "") -> Tuple[float, List[str]]:
    return (0.0, [])


def find_all_travel_distances(inputs: Dict) -> Dict[str, float]:
    return {}
